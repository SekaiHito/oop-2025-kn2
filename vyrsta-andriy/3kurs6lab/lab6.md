## Міністерство освіти і науки України  
## Львівський національний університет ветеринарії і біотехнологій імені С. З. Ґжицького  

# Звіт  
про виконання лабораторної роботи №6 з дисципліни "**Об’єктно орієнтоване програмування**"  
на тему: **Тестування коду**

Виконав: студент групи **КН-31** Вирста Андрій  
Прийняв: викл. **Назар Заплатинський**

Львів — 2025  

---

## **Мета**
Ознайомитися з основними принципами та найбільш поширеними техніками рефакторингу програмного забезпечення,  
навчитися виявляти «запахи коду» і покращувати структуру програми без зміни її функціональності.

---

## **Хід роботи**

### 1. Ознайомлення з видами тестів та пірамідою тестування
Основні види тестів

🟩 1. Unit-тести
Що перевіряють:
Маленькі частини програми — окремі функції, класи, методи.

Характеристики:
дуже швидкі
ізольовані
пишуться розробниками
основа піраміди тестування

Навіщо:
швидко знаходять помилки
дають упевненість під час рефакторингу
документують поведінку

🟧 2. Integration-тести

Що перевіряють:
як взаємодіють кілька компонентів (БД + сервер, API + логіка тощо).

Навіщо:
виявляють проблеми у взаємодії систем

🟥 3. System / End-to-End тестування

Що перевіряють:
всю систему цілком: від інтерфейсу — до бази даних.

Навіщо:
перевірити реальний сценарій користувача
важливо перед релізом

Чому тестування важливе:
мінімізує кількість багів;
підвищує якість ПЗ;
дозволяє впевнено вносити зміни;
зменшує вартість виправлення помилок;
фіксує вимоги та бізнес-логіку.
### 2. Створення Python-програми для тестування
# Клас для тестування
```py
def add(a, b):
    return a + b

# Unit-тести
import unittest

class TestMathFunctions(unittest.TestCase):
    def test_add_positive(self):
        self.assertEqual(add(2, 3), 5)

    def test_add_negative(self):
        self.assertEqual(add(-1, -1), -2)

if __name__ == "__main__":
    unittest.main()

```
### 3. Написання unit-тестів

За допомогою `unittest` були створені тести для перевірки коректності
роботи функцій.

------------------------------------------------------------------------
## **Результат виконання тестів**

Після запуску команди:
```py
    python lab6.py
```
було отримано:
```py
    ----------------------------------------------------------------------
  Ran 2 tests in 0.000s

OK

------------------------------------------------------------------------
```
## **Висновки**

У ході лабораторної роботи я ознайомився з видами тестування та
пірамідою тестування, навчився створювати unit-тести на Python з
використанням модуля `unittest`.\
Створені тести успішно проходять, що підтверджує коректність роботи
програмного коду.
