# Minesweeper (Сапер) - Курсова робота

Класична логічна гра "Сапер", реалізована мовою Python з використанням бібліотеки Tkinter та принципів об'єктно-орієнтованого програмування (ООП).

## 📋 Опис проєкту
Цей додаток є програмною реалізацією гри-головоломки, мета якої — відкрити всі клітинки на полі, що не містять мін. Проєкт демонструє використання патерну розділення логіки (GameLogic) та інтерфейсу (UI), а також реалізує рекурсивні алгоритми та роботу з файловою системою.

## 🚀 Функціональні можливості
- **3 рівні складності:**
  - Легко (7x7, 20% мін)
  - Середньо (9x9, 25% мін)
  - Складно (12x12, 30% мін)
- **Алгоритм "Безпечний старт":** Перший клік гарантовано не є міною.
- **Розумна генерація:** Випадковий розподіл мін з виключенням стартової зони.
- **Збереження прогресу:** Найкращий час (рекорд) автоматично записується у файл `scores.json`.
- **Таймер:** Відлік часу в реальному часі.

## 🛠 Технічні вимоги
Для запуску гри необхідно мати встановлене наступне програмне забезпечення:
- **Python** версії 3.8 або вище.
- Стандартні бібліотеки Python: `tkinter`, `json`, `random`, `math`, `os`, `unittest`.

## 📂 Структура проєкту
* **`main.py`** — Точка входу в програму. Запускає цикл подій.
* **`game.py`** — Клас `Minesweeper` (Controller/View). Відповідає за GUI та взаємодію з користувачем.
* **`game_logic.py`** — Клас `GameLogic` (Model). Містить математику гри, налаштування та роботу з даними.
* **`cell.py`** — Клас `Cell`. Логіка окремої клітинки та алгоритм Flood Fill.
* **`grid.py`** — Клас `Grid`. Генерація ігрового поля.
* **`window.py`** — Налаштування вікна Tkinter.
* **`tests.py`** — Модульні тести для перевірки бізнес-логіки.

## ⚙️ Інструкція із встановлення та запуску
1. Завантажте архів з проєктом або склонуйте репозиторій.
2. Відкрийте термінал (Command Prompt) у папці проєкту.
3. Запустіть гру командою:
   ```bash
   python main.py
   .