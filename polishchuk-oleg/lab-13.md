<div align="center">

# МІНІСТЕРСТВО ОСВІТИ ТА НАУКИ

<br>

## Львівський національний університет ветеринарії і біотехнологій імені С.З.Ґжицького

<br><br><br>

# Звіт

<br>

**про виконання лаборатоної роботи №13**

**з дисципліни "Об'єктно-орієнтоване програмування"**

**на тему**

## **Поведінкові шаблони проєктування (Стратегія)**

<br><br><br>

**Виконав:** студент групи КН-31<br>
Поліщук олег 

<br>

**Перевірив:** ст. викладач<br>
Назар Заплатинський

<br><br><br>

### Львів 2025
</div>

---

## 1. 🏃‍♂️ Теоретичний опис: Поведінкові шаблони (Behavioral Patterns)

**Поведінкові шаблони** — це категорія шаблонів проєктування, яка стосується **алгоритмів та розподілу відповідальності** між об'єктами.

На відміну від твірних (які створюють об'єкти) або структурних (які компонують об'єкти), поведінкові шаблони описують **схеми взаємодії та комунікації** між ними. Вони допомагають організувати складну логіку керування та зробити взаємодію об'єктів більш гнучкою, ефективною та слабко зв'язаною.

### Ключові ідеї:

* **Розподіл відповідальності:** Ці шаблони чітко визначають, який об'єкт за що відповідає в рамках певної поведінки.
* **Інкапсуляція поведінки:** Багато з цих шаблонів (як-от "Стратегія", "Команда") "загортають" поведінку в окремі об'єкти. Це дозволяє змінювати алгоритми або операції незалежно від клієнтів, які їх використовують.
* **Спрощення комунікації:** Такі шаблони, як "Посередник" (Mediator) або "Спостерігач" (Observer), допомагають організувати зв'язки між об'єктами, щоб вони не залежали один від одного напряму, а спілкувалися через централізований об'єкт або механізм підписки.

---

## 2. 📝 Індивідуальне завдання: Шаблон «Стратегія» (Strategy)

### 🔹 Теоретичний опис

**«Стратегія» (Strategy)** — це поведінковий шаблон проєктування, який **визначає сімейство алгоритмів, інкапсулює (загортає) кожен з них в окремий клас і робить їх взаємозамінними.**

Головна ідея полягає в тому, щоб дозволити клієнту (або *контексту*) **змінювати алгоритми (стратегії) під час виконання програми**, не змінюючи код самого клієнта.

#### Призначення та ідея

Шаблон пропонує винести різні варіанти алгоритму в окремі класи (стратегії), які мають спільний інтерфейс. Клас "Контекст", якому потрібна ця поведінка, зберігає посилання на один з об'єктів-стратегій і делегує йому виконання роботи. Контекст не знає про деталі реалізації стратегії; він працює з усіма стратегіями однаково через загальний інтерфейс. Це дозволяє легко додавати нові алгоритми або змінювати існуючі "на льоту".

#### ⚖️ Переваги:
* **Дотримання Принципу відкритості/закритості:** Можна додавати нові стратегії, не змінюючи код Контексту.
* **Ізоляція логіки:** Складна логіка алгоритмів виноситься з основного класу в окремі, легкі для тестування класи.
* **Гнучкість:** Дозволяє змінювати поведінку об'єкта "на льоту".

#### ⚖️ Недоліки:
* **Збільшення кількості класів:** Створюється багато дрібних класів-стратегій.
* **Клієнт має знати про стратегії:** Клієнтський код повинен розуміти, яка стратегія для чого потрібна, щоб свідомо їх обирати.

### 🔹 UML-діаграма

Діаграма класів для прикладу з навігатором (`Navigator` - Контекст) та різними стратегіями маршрутів:

```mermaid
classDiagram
    direction UP
    
    class Client {
        +main()
    }

    class Navigator {
        -strategy: IRouteStrategy
        +setStrategy(s: IRouteStrategy)
        +build_route(start, end)
    }
    note for Navigator "Клас Контекст"

    class IRouteStrategy {
        <<interface>>
        +build_route(start, end): str
    }
    note for IRouteStrategy "Інтерфейс Стратегії"

    class WalkingStrategy {
        +build_route(start, end): str
    }
    
    class CarStrategy {
        +build_route(start, end): str
    }

    class TransitStrategy {
        +build_route(start, end): str
    }

    %% Зв'язки
    Client ..> Navigator : "використовує"
    Client ..> WalkingStrategy : "створює"
    Client ..> CarStrategy : "створює"
    
    Navigator o-- IRouteStrategy : "має (стратегію)"
    
    WalkingStrategy --|> IRouteStrategy : "реалізує"
    CarStrategy --|> IRouteStrategy : "реалізує"
    TransitStrategy --|> IRouteStrategy : "реалізує"







##приклад коду 
    ##import abc

# --- 1. Інтерфейс Стратегії ---
class IRouteStrategy(abc.ABC):
    """
    Абстрактний базовий клас (інтерфейс) для всіх стратегій
    побудови маршруту.
    """
    @abc.abstractmethod
    def build_route(self, start_point: str, end_point: str) -> str:
        """Побудувати маршрут і повернути його опис."""
        pass

# --- 2. Конкретні Стратегії ---
class WalkingStrategy(IRouteStrategy):
    """Стратегія для пішохода (найкоротший шлях)."""
    def build_route(self, start_point: str, end_point: str) -> str:
        # Уявна складна логіка розрахунку...
        return (f"Пішохідний маршрут (найкоротший) з {start_point} до {end_point}. "
                f"Дистанція: 2 км.")

class CarStrategy(IRouteStrategy):
    """Стратегія для автомобіля (найшвидший шлях)."""
    def build_route(self, start_point: str, end_point: str) -> str:
        # Уявна складна логіка з урахуванням трафіку...
        return (f"Автомобільний маршрут (найшвидший) з {start_point} до {end_point}. "
                f"Дистанція: 3.5 км. Час: 10 хв (з урахуванням заторів).")

class TransitStrategy(IRouteStrategy):
    """Стратегія для громадського транспорту."""
    def build_route(self, start_point: str, end_point: str) -> str:
        # Уявна складна логіка з розкладом...
        return (f"Транзитний маршрут з {start_point} до {end_point}. "
                f"Автобус №25 -> Метро 'Зелена гілка'. Час: 25 хв.")

# --- 3. Контекст ---
class Navigator:
    """
    Клас "Контекст", який використовує об'єкт-стратегію.
    """
    def __init__(self, strategy: IRouteStrategy):
        # Контекст отримує стратегію при створенні...
        self._strategy = strategy
        print(f"Навігатор створено. Початкова стратегія: {strategy.__class__.__name__}")

    def set_strategy(self, strategy: IRouteStrategy):
        """Метод для зміни стратегії "на льоту"."""
        print(f"\n[Навігатор] Зміна стратегії на: {strategy.__class__.__name__}")
        self._strategy = strategy

    def build_route(self, start: str, end: str):
        """
        Контекст не виконує логіку сам, 
        а делегує її поточному об'єкту-стратегії.
        """
        print(f"[Навігатор] Запит на побудову маршруту з {start} до {end}...")
        route_details = self._strategy.build_route(start, end)
        print("--- Ваш маршрут ---")
        print(route_details)
        print("-------------------")


# --- 4. Клієнтський код ---

# Створюємо конкретні об'єкти-стратегії
walk_strategy = WalkingStrategy()
car_strategy = CarStrategy()
transit_strategy = TransitStrategy()

# Клієнт обирає початкову стратегію (наприклад, для авто)
navigator = Navigator(car_strategy)

# І використовує її
navigator.build_route("Дім (вул. Зелена, 15)", "Офіс (вул. Головна, 1)")

# Клієнт змінює стратегію в навігаторі на пішохідну
navigator.set_strategy(walk_strategy)
navigator.build_route("Дім (вул. Зелена, 15)", "Офіс (вул. Головна, 1)")

# Клієнт змінює стратегію на транзитну
navigator.set_strategy(transit_strategy)
navigator.build_route("Офіс (вул. Головна, 1)", "Дім (вул. Зелена, 15)")
