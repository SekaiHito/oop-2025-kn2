<div align="center">

# МІНІСТЕРСТВО ОСВІТИ ТА НАУКИ

<br>

## Львівський національний університет ветеринарії і біотехнологій імені С.З.Ґжицького

<br><br><br>

# Звіт

<br>

**про виконання лаборатоної роботи №12**

**з дисципліни "Об'єктно-орієнтоване програмування"**

**на тему**

## **Принципи проєктування SOLID**

<br><br><br>

**Виконав:** студент групи КН-31<br>
[Ваше Прізвище та Ім'я]

<br>

**Перевірив:** ст. викладач<br>
Назар Заплатинський

<br><br><br>

### Львів 2025
</div>

---

## 1. 💡 Загальний опис принципів проєктування

**Принципи проєктування** — це набір високорівневих рекомендацій, порад та найкращих практик для написання програмного коду. Це не конкретні алгоритми чи готові "шаблони", а скоріше **філософія** гнучкого та якісного дизайну системи.

Вони допомагають розробникам приймати правильні архітектурні рішення, щоб уникнути поширених проблем у розробці.

### 🎯 Головні цілі принципів проєктування:

1.  **Гнучкість (Flexibility):** Систему має бути легко змінювати або розширювати, додаючи нову функціональність.
2.  **Підтримуваність (Maintainability):** Код має бути легким для розуміння, виправлення помилок (дебаггінгу) та модифікації.
3.  **Повторне використання (Reusability):** Компоненти системи мають бути достатньо незалежними, щоб їх можна було використовувати в інших проєктах.
4.  **Надійність (Robustness):** Система має бути стійкою до помилок та непередбачуваних ситуацій.

### ⚙️ Проблеми, які вони вирішують

Дотримання принципів проєктування допомагає уникнути так званих "запахів коду" (code smells), які роблять систему **жорсткою** (важко змінити), **крихкою** (зміни ламають старий код) та **нерухомою** (код важко використати повторно).

### Найвідоміші набори принципів:

Найчастіше, коли говорять про принципи проєктування, мають на увазі два ключові набори:

1.  **SOLID:** П'ять основних принципів об'єктно-орієнтованого дизайну.
2.  **KISS (Keep It Simple, Stupid):** "Роби простіше".
3.  **DRY (Don't Repeat Yourself):** "Не повторюйся".
4.  **YAGNI (You Ain't Gonna Need It):** "Вам це не знадобиться".

---

## 2. 📝 Детальний опис: Принцип відкритості/закритості (OCP)

Це другий принцип з абровеатури **SOLID** — **O (Open/Closed Principle)**.

### 🔹 Теоретичний опис

> **Формулювання:** Програмні сутності (класи, модулі, функції) повинні бути **відкриті для розширення**, але **закриті для модифікації**.

#### Що це означає?

* **"Закриті для модифікації":** Коли клас вже написаний, протестований і працює, ви **не повинні змінювати його вихідний код**, щоб додати нову поведінку. Будь-яка зміна в існуючому коді — це ризик зламати те, що вже працювало.
* **"Відкриті для розширення":** Ви повинні мати можливість **додавати нову функціональність** до системи, не чіпаючи старий код.

Ключова ідея для досягнення цього — **абстракція**. Код прив'язується не до конкретних класів, а до **інтерфейсів**. Нова функціональність додається шляхом створення **нових класів**, які реалізують цей спільний інтерфейс. Цей принцип лежить в основі багатьох шаблонів, наприклад, **"Стратегія"**.

### 🔹 Приклад коду (на Python)

Розглянемо клас `OrderProcessor`, який розраховує вартість доставки.

#### 👎 Поганий приклад (порушення OCP)

Якщо ми додаємо нові методи доставки через `if/elif`, ми змушені **модифікувати** існуючий клас, ризикуючи зламати його.

```python
class OrderProcessor:
    def __init__(self, order):
        self.order = order

    def get_shipping_cost(self, shipping_method: str):
        """
        Цей метод ПОРУШУЄ OCP.
        """
        if shipping_method == "nova_poshta":
            cost = self.order.weight * 1.5 + 40
            return cost
        elif shipping_method == "ukrposhta":  # <-- МОДИФІКАЦІЯ
            cost = self.order.weight * 0.8 + 25
            return cost
        elif shipping_method == "meest":  # <-- ЗНОВУ МОДИФІКАЦІЯ
            cost = self.order.weight * 1.1 + 30
            return cost
        else:
            raise ValueError("Невідомий метод доставки")


##👍 Гарний приклад (дотримання OCP)
Ми виносимо логіку розрахунку в окремі стратегії, які реалізують спільний інтерфейс.

Крок 1: Створюємо абстракцію (інтерфейс)

Python

from abc import ABC, abstractmethod

class IShippingStrategy(ABC):
    """
    Інтерфейс для всіх стратегій розрахунку доставки.
    """
    @abstractmethod
    def calculate_cost(self, order) -> float:
        pass
Крок 2: Створюємо конкретні реалізації (стратегії)

Python

class NovaPoshtaStrategy(IShippingStrategy):
    """Реалізація для Нової Пошти."""
    def calculate_cost(self, order) -> float:
        cost = order.weight * 1.5 + 40
        print(f"[Нова Пошта] Вартість: {cost} грн")
        return cost

class UkrPoshtaStrategy(IShippingStrategy):
    """Реалізація для Укрпошти."""
    def calculate_cost(self, order) -> float:
        cost = order.weight * 0.8 + 25
        print(f"[Укрпошта] Вартість: {cost} грн")
        return cost
Крок 3: Клас "Контекст" (закритий для модифікацій) Клас OrderProcessor тепер закритий. Він не знає про "Нову Пошту" чи "Укрпошту", а лише про інтерфейс IShippingStrategy.

Python

class Order: # Допоміжний клас
    def __init__(self, weight):
        self.weight = weight

class OrderProcessor:
    def __init__(self, order: Order):
        self.order = order

    def get_shipping_cost(self, strategy: IShippingStrategy):
        """
        Клас делегує розрахунок об'єкту-стратегії.
        Цей код не треба буде змінювати НІКОЛИ.
        """
        cost = strategy.calculate_cost(self.order)
        return cost
Крок 4: Розширення системи (відкритість для розширення) Коли нам потрібен новий метод доставки, ми не чіпаємо старий код, а просто додаємо новий клас.

Python

class MeestStrategy(IShippingStrategy):
    """
    НОВА ФУНКЦІОНАЛЬНІСТЬ (РОЗШИРЕННЯ).
    Ми нічого не зламали у старому коді.
    """
    def calculate_cost(self, order) -> float:
        cost = order.weight * 1.1 + 30
        print(f"[Meest] Вартість: {cost} грн")
        return cost
