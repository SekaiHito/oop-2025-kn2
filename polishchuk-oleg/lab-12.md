<div align="center">

# МІНІСТЕРСТВО ОСВІТИ ТА НАУКИ

<br>

## Львівський національний університет ветеринарії і біотехнологій імені С.З.Ґжицького

<br><br><br>

# Звіт

<br>

**про виконання лаборатоної роботи №11**

**з дисципліни "Об'єктно-орієнтоване програмування"**

**на тему**

## **Структурні шаблони проєктування (Компонувальник)**

<br><br><br>

**Виконав:** студент групи КН-31<br>
Поліщук Олег 

<br>

**Перевірив:** ст. викладач<br>
Назар Заплатинський

<br><br><br>

### Львів 2025
</div>

---

## 1. 🧩 Теоретичний опис: Структурні шаблони (Structural Patterns)

**Структурні шаблони** — це категорія шаблонів проєктування, яка описує, **як класи та об'єкти можуть бути об'єднані для формування більших, складніших структур**.

Головна мета цієї групи шаблонів — спростити проєктування, ідентифікуючи прості способи реалізації зв'язків між сутностями. Вони зосереджені на тому, як класи та об'єкти **компонуються** (складаються разом), щоб отримати нову функціональність.

### Ключові ідеї:

* **Композиція та спадкування:** Ці шаблони активно використовують механізми композиції (включення об'єктів один в одного) та спадкування для побудови нових функціональностей.
* **Гнучкість:** Дозволяють змінювати структуру системи (наприклад, додавати нові обов'язки об'єктам або "з'єднувати" несумісні класи), не змінюючи код, який її використовує.
* **Спрощення:** Допомагають приховати складність системи за простішим інтерфейсом ("Фасад") або зробити сумісними класи, які не планувалися для спільної роботи ("Адаптер").

---

## 2. 📝 Індивідуальне завдання: Шаблон «Компонувальник» (Composite)

### 🔹 Теоретичний опис

**«Компонувальник» (Composite)** — це структурний шаблон проєктування, який дозволяє **об'єднувати об'єкти в деревоподібні структури** для представлення ієрархій "частина-ціле".

Ключова ідея шаблону полягає в тому, щоб клієнтський код міг **однаково працювати як з окремими об'єктами (листками), так і з їхніми групами (контейнерами)**.

#### Призначення та ідея

Уявіть, що ви моделюєте файлову систему. У вас є `Файли` (прості об'єкти) і `Папки` (складні об'єкти, які можуть містити і `Файли`, і інші `Папки`). Якщо клієнтський код захоче, наприклад, дізнатися розмір, йому доведеться писати різну логіку: для файлу просто запитати `file.getSize()`, а для папки — запустити цикл, пройти по всьому вмісту і просумувати розміри.

Шаблон «Компонувальник» вирішує це, створюючи загальний **інтерфейс (Компонент)**, наприклад, `FileSystemItem`, з методом `getSize()`.
* **Листок (Leaf)**: `Файл` реалізує цей метод і просто повертає свій розмір.
* **Контейнер (Composite)**: `Папка` також реалізує цей метод. Всередині вона викликає `getSize()` у *кожного* свого дочірнього елемента (неважливо, файл це чи інша папка) і повертає їхню суму.

Таким чином, клієнт просто викликає `item.getSize()` і не замислюється, чи це один файл, чи ціла структура папок.

#### ⚖️ Переваги:
* **Уніфікований інтерфейс:** Клієнт працює з простими та складними об'єктами однаково.
* **Гнучкість:** Легко додавати нові види "листків" та "контейнерів" у систему.
* **Дотримання Принципу відкритості/закритості:** Можна додавати нові компоненти, не змінюючи код, який з ними працює.

#### ⚖️ Недоліки:
* **Занадто загальний інтерфейс:** Іноді доводиться створювати інтерфейс, який не має сенсу для "листків" (наприклад, `add(component)`).

### 🔹 UML-діаграма

Діаграма класів для прикладу з графічними примітивами:

```mermaid
classDiagram
    direction UP
    
    %% Абстрактний Компонент
    class Graphic {
        <<interface>>
        +draw()
        +add(g: Graphic)
        +remove(g: Graphic)
    }

    %% Листок (Leaf)
    class Circle {
        +draw()
        +add(g: Graphic)  --* (порожня реалізація)
        +remove(g: Graphic) --* (порожня реалізація)
    }
    
    %% Листок (Leaf)
    class Square {
        +draw()
        +add(g: Graphic)
        +remove(g: Graphic)
    }

    %% Компонувальник (Composite)
    class Picture {
        -children: list~Graphic~
        +draw()
        +add(g: Graphic)
        +remove(g: Graphic)
    }
    
    %% Зв'язки
    Circle --|> Graphic : "реалізує"
    Square --|> Graphic : "реалізує"
    Picture --|> Graphic : "реалізує"
    
    %% Компонувальник "має" компоненти
    Picture o-- "0..*" Graphic : "містить"
    
    class Client {
    }
    Client ..> Graphic : "використовує"
